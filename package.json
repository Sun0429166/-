{
  "name": "statement-skill",
  "version": "0.1.0",
  "description": "A simple statement-writing skill (Node + TypeScript) for agent integration",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint . --ext .ts"
  },
  "author": "Sun0429166",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.23.0",
    "fastify-cors": "^8.2.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
